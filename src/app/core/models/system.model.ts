export interface System {
    buildRevision: string,
    buildTimestamp: number;
    buildVersion: string,
    productName: string
    serverUniq: string;
    serverVersion: string
    vendorName: string
}